#!/usr/bin/env python3
import os
import sys
from pathlib import Path

sys.dont_write_bytecode = True

REPO = Path(__file__).resolve().parent
os.chdir(REPO)
sys.path.insert(0, str(REPO / "src" / "scripts"))

from cli import run_uninstall

# Classic confirm-and-flags flow — never enters the TUI wizard.
raise SystemExit(run_uninstall(sys.argv[1:], prog="legacy-uninstall"))
